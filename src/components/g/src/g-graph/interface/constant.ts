const globalPrefix = 'g-graph'

export const globalConstants = {
  globalPrefix,
  graphRootId: `${globalPrefix}-root`,
  rootContainerClassName: 'root-container',
  nodeGroupName: `${globalPrefix}-node-group`,
  edgeGroupName: `${globalPrefix}-edge-group`,
}
